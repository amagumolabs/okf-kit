<!--
This file always exists, but it does not always need content.

Write a real design only if at least one applies:
- cross-cutting change (multiple services/modules) or a new architectural pattern
- a new external dependency, or significant data model changes
- security, performance, or migration complexity
- ambiguity worth settling before code is written

If none apply, delete every section below and leave exactly one line:

    Not required because <specific reason>.

That is the same rule the rest of this schema uses: never skip a file, always
state the reason. It keeps a record that the question was considered rather than
forgotten, and it keeps propose running without a detour.
-->

## Context

<!-- Background, current state, constraints, stakeholders -->

## Goals / Non-Goals

**Goals:**
<!-- What this design achieves -->

**Non-Goals:**
<!-- Explicitly out of scope -->

## Decisions

<!--
Key technical choices with rationale: why X over Y, alternatives considered.

A decision that will still matter after this change ships should be promoted to
`.okf/decisions/` during the verification pass - otherwise archiving buries it.
-->

## Risks / Trade-offs

<!-- [Risk] -> Mitigation -->

## Migration Plan

<!-- Deploy steps, rollback strategy. Omit if not applicable. -->

## Open Questions

<!-- Outstanding decisions or unknowns. Omit if none. -->
